<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />

    <application>
        <!-- Hidden CarAppActivity â€” exists solely to trigger Templates Host binding.
             NOT a launcher. Lives in separate task. Excluded from recents.
             MainActivity remains the primary UI. -->
        <activity
            android:name="androidx.car.app.activity.CarAppActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity="zeno.carlink.templates"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <meta-data
                android:name="distractionOptimized"
                android:value="true" />
        </activity>

        <!-- Cluster Navigation CarAppService (headless â€” no UI on main display) -->
        <service
            android:name=".cluster.CarlinkClusterService"
            android:enabled="false"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
                <category android:name="androidx.car.app.category.FEATURE_CLUSTER" />
            </intent-filter>
            <meta-data
                android:name="androidx.car.app.minCarApiLevel"
                android:value="6" />
            <meta-data
                android:name="com.gm.homescreen.visibility"
                android:value="HIDDEN" />
        </service>

        <meta-data
            android:name="com.android.automotive"
            android:resource="@xml/automotive_app_desc" />

        <!-- Required at application level â€” Templates Host reads minCarApiLevel
             from application metadata, not from service element -->
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="6" />

        <!-- Shim for Templates Host's unregistered ClusterIconContentProvider.
             Claims the orphaned authority so icon delivery works for cluster display. -->
        <provider
            android:name=".provider.ClusterIconShimProvider"
            android:authorities="com.google.android.apps.automotive.templates.host.ClusterIconContentProvider"
            android:exported="true"
            android:grantUriPermissions="true"
            tools:ignore="ExportedContentProvider" />
    </application>

</manifest>
