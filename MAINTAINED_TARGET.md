# Maintained Target

The active T7 troubleshooting and implementation target is `carlink_flutter_upstream`, package `com.test.fudge`.

The sibling native `app/` path is not part of the current USB permission and CarPlay-connect work. Keep future T7 connection fixes focused on the upstream Flutter project unless device validation proves otherwise.
